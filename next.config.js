/** @type {import('next').NextConfig} */

const nextConfig = {
    images: {
        domains: ['www.costavida.com', 'images.squarespace-cdn.com', 'wallpaperswide.com', 'previews.123rf.com', 'localhost']
    }
}

module.exports = nextConfig
